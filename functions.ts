function addTwo (num: number) {
    // num.toUpperCase()  now we can't perform string or any other datatypes operations on this number
    return num + 2; 
}

function getUpper(val: string) {
    return val.toUpperCase()
}

function signUpUser(name: string, email: string, isPaid: boolean) {
    return 
}

let loginUser = (name: string , email: string, isPaid: boolean = false) => {

}
addTwo(5)
getUpper("two")
signUpUser("gourav", "gou@gmail.com", true)
loginUser("h", "gou@gmail.com")

export {}